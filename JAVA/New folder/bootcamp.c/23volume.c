#include<stdio.h>

    int main(){
    int r,n,m;
    printf("enter the integer:\n");
    scanf("%d %d",&n,&m);
    if (n>m)
    {
        printf("volume up %d",r=m-n);
    }
    else{
        printf("volume down %d",r=m-n);
    }
return 0;
}