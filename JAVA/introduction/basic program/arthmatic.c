#include<stdio.h>
int main(){
    int l,b,a;
    printf("what is the length and bredth of rectangle\n");
    scanf("%d%d",&l,&b);
    a=l*b;
    printf("area of rectangle is %d",a);
    return 0;
    }
