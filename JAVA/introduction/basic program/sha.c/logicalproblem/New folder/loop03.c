
#include<stdio.h>
int main(){

int a,b,s,x;
    float c,d,w,u;
    printf("enter the two number\n");
    scanf("%d%d",&a,&b);
    printf("enter the two number\n");
    scanf("%f%f",&c,&d);
    for(int value){
        printf("sum of two number is %d",s==a+b);
        printf("difference of two number is %d",x=a-b);
    }
    for(float value ){
        printf("sum of two number is %f",u=c+d);
        printf("difference of two number is %f",w==c-d);
    }
    
    
    
    
    return 0;
}